# SyncTV Redis Compose Environment
#
# Copy to .env.redis before running docker compose.
# make compose-init generates this file automatically.
#
# SECURITY WARNING: Never commit .env.redis with real secrets.

REDIS_PASSWORD=
