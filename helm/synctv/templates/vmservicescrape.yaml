{{- if and .Values.metrics.enabled .Values.metrics.vmServiceScrape.enabled }}
{{- $vmServiceScrapeNamespace := .Values.metrics.vmServiceScrape.namespace | default .Release.Namespace }}
{{- $metricsAuthMode := .Values.metrics.auth.mode | default "bearer_token" }}
{{- if and (ne $vmServiceScrapeNamespace .Release.Namespace) (or .Values.metrics.tls.enabled (ne $metricsAuthMode "kubernetes")) }}
{{- fail "metrics.vmServiceScrape.namespace must match the release namespace when metrics TLS is enabled or metrics.auth.mode is bearer_token/basic because VMServiceScrape Secret references are namespace-scoped; use metrics.auth.mode=kubernetes for cross-namespace VMServiceScrape objects, or leave metrics.vmServiceScrape.namespace empty" }}
{{- end }}
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: {{ include "synctv.fullname" . }}
  namespace: {{ $vmServiceScrapeNamespace }}
  labels:
    {{- include "synctv.labels" . | nindent 4 }}
    {{- with .Values.metrics.vmServiceScrape.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      {{- include "synctv.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: metrics
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.metrics.vmServiceScrape.interval | default "30s" }}
      {{- with .Values.metrics.vmServiceScrape.scrapeTimeout }}
      scrapeTimeout: {{ . }}
      {{- end }}
      {{- if .Values.metrics.tls.enabled }}
      scheme: https
      tlsConfig:
        serverName: {{ include "synctv.metricsTlsServerName" . }}
        ca:
          secret:
            name: {{ include "synctv.metricsTlsSecretName" . }}
            key: ca.crt
      {{- end }}
      {{- if eq (.Values.metrics.auth.mode | default "bearer_token") "kubernetes" }}
      bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
      {{- else if eq (.Values.metrics.auth.mode | default "bearer_token") "bearer_token" }}
      authorization:
        type: Bearer
        credentials:
          name: {{ include "synctv.secretName" . }}
          key: SYNCTV_METRICS_AUTH_BEARER_TOKEN
      {{- else if eq (.Values.metrics.auth.mode | default "bearer_token") "basic" }}
      basicAuth:
        username:
          name: {{ include "synctv.secretName" . }}
          key: SYNCTV_METRICS_AUTH_BASIC_USERNAME
        password:
          name: {{ include "synctv.secretName" . }}
          key: SYNCTV_METRICS_AUTH_BASIC_PASSWORD
      {{- end }}
{{- end }}
