# SyncTV PostgreSQL Compose Environment
#
# Copy to .env.postgres before running docker compose.
# make compose-init generates this file automatically.
#
# SECURITY WARNING: Never commit .env.postgres with real secrets.

POSTGRES_USER=synctv
POSTGRES_PASSWORD=
POSTGRES_DB=synctv
